# Minimal InsightHire Requirements - Essential packages only
# Use this if the full requirements.txt has compatibility issues

# Core web framework
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Basic ML and CV (most compatible versions)
numpy>=1.24.0,<1.27.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Firebase (essential for backend)
firebase-admin>=6.0.0

# Optional: Install these separately if needed
# tensorflow>=2.15.0
# scikit-learn>=1.3.0
# librosa>=0.10.0
# mediapipe>=0.10.0
